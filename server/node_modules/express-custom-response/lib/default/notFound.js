
module.exports = function (message, code, extraData) {
    // Set status code
    this.status(404);
    this.send({
                  message   : message,
                  code      : code,
                  extraData : extraData
              });
}
